export interface GridVars {
  maxWidth?: number

  desktop: {
    gutter: number
    columns: number
    margin: number
  }

  tablet: {
    gutter: number
    columns: number
    margin: number
  }
	
  phone: {
    gutter: number
    columns: number
    margin: number
  }
}
